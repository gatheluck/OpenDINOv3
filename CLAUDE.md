# OpenDINOv3 — 作業規約

このファイルは毎セッション自動で読み込まれる。**ここに書いていないルールは守られない。**

目的: **大規模データセットを高速にダウンロードし、事実とベストプラクティスに基づいて、
様々なモデルの学習に使いやすい形で ABCI 上に構築する。**

## 絶対に忘れてはならない3つ

1. **事実に基づいて進める**（§1）
2. **厳しいテスト駆動開発で進める**（§2）
3. **本質を見失わない**（§0）— 今やっていることと目的を常に突き合わせる
4. **ABCI とのやりとりを最小化する**（§0.1）— 渡す前に全経路をローカルで検証する

---

## 0. 本質を見失わない（最優先）

**着手前に必ず問う: これは目的にとって必要か。**

判定基準は1つ。**その結果によって、次にやることが変わるか。**
変わらないなら、それは今やる作業ではない。

### 実際に道を外した記録

| やったこと | なぜ不要だったか |
|---|---|
| DINOv3 の 256px を合否ゲート扱いし、事前フィルタを提案した | 目的は汎用資産。特定モデルの入力サイズは合否ではない。フィルタは全利用者の選択肢を不可逆に奪う |
| 前任者ツリーとメタデータ申告値の照合ツールを、パイロットの前に作った | **フィルタしないと決まった時点で、サイズ分布の精度はどの判断も動かさない。** 本番パイプラインは前任者ツリーを一切読まない（`OD_ROOT` への参照が production 系スクリプトに存在しない）。同じ問いにはパイロットが自前データでより正確に答える |

### 外れやすい型

- **精度を上げる作業**: 既に決定に十分な精度があるのに、さらに測る
- **他人のデータを測る作業**: 自分の成果物が同じ問いに答えるのに、先に他人のものを測る
- **特定モデルへの最適化**: 汎用資産に単一利用者の要件を焼き込む

### ユーザーが方向を問うたら、まず止まって検算する

「これは本当に必要ですか」と聞かれたら、擁護せずコードで確かめる。
上表の2件はどちらもこの問いで発見された。**問われる前に自分で問うこと。**

---

## 0.1 ABCI とのやりとりを最小化する

**ABCI 上での実行は時間を要する。1往復を無駄にしない。**

### やってはいけない: 逐次発見

ブロッカーを1つ見つける → 直す → 渡す → 次のブロッカーが出る、を繰り返さない。
実際にこれをやって、ユーザーを何往復も足止めした:

| 往復 | 出たもの |
|---|---|
| 1 | `$OD_REPO` が存在せず `cd` が `$HOME` へ |
| 2 | plan のパスが `/corpus/...` で本番ジョブから開けない |
| 3 | `OD_BLUR_FACES` が生成ジョブに入らない |
| 4 | `--env` の一覧に `OD_BLUR_FACES` と `OD_META_ROOT` が無い |

**4件とも、渡す前にソースを端から端まで読めば1回で全部見つかった。**

### 渡す前に必ずやること

1. **経路全体を読む。** 一部ではなく、投入から実行までの全スクリプトを通しで。
   環境変数は「どこで設定され、どこで消費されるか」を1つずつ突き合わせる。
   特に `singularity exec --env` は**明示列挙**なので、export しただけでは届かない。
2. **ローカルで end-to-end リハーサルを走らせる。**
   `tests/stubs/singularity_docker` は実 Docker にマップするので、バインドと環境変数の
   欠落が本番と同じ形で出る。ホスト環境を継承しない分、Singularity より厳しい。
3. **失敗しうる箇所を列挙してから渡す。** 「たぶん動く」で渡さない。

### 1往復に詰め込む

確認が複数必要なら、1つのコマンドにまとめる。順に投げて待つのは往復の無駄。

---

## 1. 事実に基づく（最優先）

**確認していないことを断定しない。** 「〜のはず」「〜のままです」と書きそうになったら、
それは未確認のサインである。

### 状態は記憶で語らない。必ず問い合わせる

自分がさっきやったことでも、状態は変わっている。実害4回:

| 断定した内容 | 実際 | 確認コマンド |
|---|---|---|
| このリポジトリは public | **private** | `gh repo view` |
| push した | ブランチ確認漏れで **no-op**（2回） | `git ls-remote origin <branch>` と HEAD の照合 |
| PR は未マージ | **マージ済み** | `gh pr list --state merged` |

**ルール: PR・ブランチ・リモート・ジョブの状態を述べる文には、
それを出したコマンドの出力を同じメッセージ内に添える。** 出力が無い主張は書かない。


### 調べる順序 — 安い情報源から尽くす。観測は最後の確認

| 順 | 手段 | コスト |
|---|---|---|
| 1 | 公式ドキュメント・**公開リポジトリのソース**・Web 検索 | 無料・即座 |
| 2 | ローカル実行とテスト | 数秒 |
| 3 | ABCI ログインノード | **往復1回**（人を待たせる） |
| 4 | ジョブ投入 | **数時間の待ち** |

**上から順に尽くしてから下に行く。**

実害: DataComp のキャプション列名は公式リポジトリの `download_upstream.py` に
`caption_col="text"` と書かれていた。検索1回で済むのに、先にクラスタで
スキーマを見てもらおうとした。

**「事実に基づく」とは、あらゆる事実源を漁った上で、最後に実測で確認すること。**
1つの情報源で止めない。公式ドキュメントに無くても、**公開実装のソースには
書いてある**ことがある（ABCI の `qgstat` も、DataComp の `caption_col` もそう）。

### 公式ドキュメントを先に読む

推測やプローブより先に、公式ドキュメントを引く。実害2回:

- SIF がマウントできない → 答えは `docs.abci.ai/v3/ja/containers/` に明記されていた
  （`SINGULARITY_TMPDIR` はノードローカルのみ。`$HOME` と `/groups` は不可）
- 「誰がノードを使っているか」→ ABCI 固有の `qgstat` / `qrstat` / `nodestatus` を
  知らずに `qstat` のプローブを組み立てた

**サイト固有のラッパーが存在し、標準コマンドは制限されていることがある。**
調べた結果は「記載あり／なし」を明示して報告する。

### エラーメッセージを鵜呑みにしない、ただし読む

- 「compression algorithm or the image is corrupted」→ 実際は領域不足
- 一方 `cp: ... are the same file` は文字通りで、前回実行の残骸が原因だった

---

## 2. テスト駆動開発（最優先）

### ケースではなくクラスをテストする

このプロジェクトのバグは**ほぼ全て部品の継ぎ目**で起きた。部品は単体で正しかった。

| 実害 | 原因 | テストが見逃した理由 |
|---|---|---|
| 入力形式の誤認（parquet を txt と仮定） | データを確認せず実装 | 形式を仮定したテストしか無い |
| シャード数が並列度を上限で抑える | img2dataset の分配単位を未確認 | 並列度の指定値しか見ていない |
| 絶対 symlink がバインドマウントで壊れる | — | **job スクリプトを一度も実行していなかった** |
| 前回の残骸に `cp` が失敗 | — | e2e テストが**空ディレクトリに1回だけ**実行 |
| ノードローカル領域が node1 に無い | — | ローカルは1ノード。かつ `PBS_LOCALDIR` を共有ツリー内に置いていた |

**書くべきは、ノード数や実行回数によらず成り立つ不変条件。**

- bind の元は**共有ストレージか、そのスクリプト自身が作るか**
- ステージングは**冪等**（再実行は常に前回のファイルがある状態から始まる）
- staged パスに**絶対 symlink を作らない**。ツリーを移動しても動く
- **取りこぼしがあったフェーズは判定を出せない**

### テストを書いたら、必ず落ちることを確認する

**変異テストを省略しない。** このプロジェクトで、書いたテストが
**バグのある状態で通ってしまった**ことが2回ある。

```bash
# 実装を1箇所壊す → 対応するテストが落ちることを確認 → 戻す
```

置換が実際に適用されたかも確認する。`sed` の `\|` は GNU 拡張で
**BSD sed（macOS）では無音で失敗する**。変異が効いていないのに
「検出されなかった」と誤読した実例がある。

grep で結果を判定するときは ANSI エスケープを除去する:
`sed 's/\x1b\[[0-9;]*m//g'`

**変異検出ハーネス自体が壊れる。** 実例3件、いずれも「全部検出されなかった」
「何も検出されなかった」という**安全側に嘘をつく**形で出た:

| 壊れ方 | 症状 |
|---|---|
| `grep -E '[0-9]+ failed'` で判定 | 本プロジェクトの `addopts = "-q"` が集計行を抑制するため常に不一致。全変異が「未検出」 |
| bind mount が古い状態を返す | コンテナが未変異のソースを読む。同じ変異が回によって検出されたりされなかったり |
| ベースラインを `失敗数 == 0` で判定 | コンテナが起動失敗しても0件なので「緑」。何も走っていない |

対策: 判定は `^FAILED` 行で行う。ツリーは bind mount せず**コンテナ内にコピー**する
（`-v "$PWD:/src:ro"` → `cp -r /src /tmp/w`）。ベースラインは
**成功数が1件以上あること**まで確認する。

### 導出値を実測値と呼ばない。導出値でハードリミットを決めない

**実害。** 最初の本番ウェーブ8サブジョブ（約16ノード時間）が walltime で
全滅した。原因は技術的な不具合ではなく、**数字の出所を確認しなかったこと**。

```
# scripts/submit_production.sh にあった記述
OD_PROD_WALLTIME  default 02:00:00 (a task measured 1.01 h)
```

**1.01時間を測ったタスクは存在しない。** これは `650000 / SUCC_PER_SEC / 3600`
という計算結果であって、観測ではない。そして `SUCC_PER_SEC = 180` 自体が
前任者の実行時の値で、**このセッションの引き継ぎメモに「絶対スループットが
履歴値より3.5倍遅い」と未解決事項として書かれていた**。
つまり「怪しい」と分かっている数字から walltime という**強制終了の閾値**を
決めた。実測は22.5 URL/秒/ノードで、モデルの**12分の1**だった。

**規則:**

1. **定数には出所と日付を書く。** 「measured」と書くなら、何を・いつ・
   どの条件で測ったか。書けないなら derived と書く。
2. **導出値からハードリミット（walltime、タイムアウト、閾値）を決めない。**
   導出が外れたときに、限界を超えるのではなく余裕が減るだけになる側に倒す。
3. **未検証の仮定が1つでもあるなら、初回は「外れても結果が残る」寸法にする。**
   パイロットの目的が未知数の測定なら、その未知数から walltime を決めては
   いけない。外れた瞬間に測定そのものが失われる。
   今回なら walltime は予約上限いっぱい、またはタスクを10倍外しても
   終わる大きさにすべきだった。
4. **リハーサルは正しさを見るが速さは見ない。** 24枚のリハーサルは
   パス・環境変数・列・ぼかしを全部通したが、スループットには無力だった。
   **正しさと容量は別のクラスの failure。** 通ったからといって
   「準備完了」と言わない。

### 出力の部分文字列で assert しない

pytest の `tmp_path` は**テスト関数名を含む**。ツールが処理対象ディレクトリを
表示すると、その名前が stdout に出る。

実例: `test_a_divergence_from_the_claim_is_called_out` は
`assert "diverge" in combined.lower()` で判定していたが、これは
**tmp_path のパスにマッチしていた**。ツールの判定を完全に無効化しても通り続けた。
同様に `..._status_column_...` は `"status"`、`"failed"` は
`"failed or sizeless"` という定型文にマッチしていた。3件が同じ理由で偽陽性。

assert は次のいずれかにする:
- `--json` の構造化出力のフィールド値
- 終了コード
- 衝突し得ない完全な文字列（`"claimed      550"` のような整形済みの並び）

### 検証はコンテナ内で行う

```bash
docker run --rm -v "$PWD:/w" -w /w opendinov3:test python -m pytest -p no:cacheprovider
```

ホストの `python3` には pyarrow が無い。ローカルで通らないものを ABCI に送らない。
**変異テストのときだけは bind mount ではなくコピーを使う**（上記）。

---

## 3. ABCI の使い方

**律速は計算時間ではなく「投入回数 × 待ち時間」。** 待ちは数時間規模。

1. **ノードが要る問いだけを投げる。** 既存ファイルを読むだけの調査は
   ログインノードで完結する
2. **独立な問いは並行して投げる。** ただし依存のあるジョブは投げない
3. **1ジョブに入るだけ問いを詰める。** 危険な処理は後ろに置く
4. **ローカルで潰せるものは投入前に潰す**
5. **投入済みのジョブは触らない。** `qdel` は投入時刻をリセットする

### 分散が無料と判明している（実験0003）

2ノード×16プロセスは1ノード×32プロセスの **0.97倍**、DNS も 6.0% vs 6.1%。
したがって**マルチノードジョブを使う理由が無い**。独立した1ノードジョブ N 本の方が
スケジュールされやすく、`pbsdsh`・ノード間ステージング・共有スクラッチが不要
（これらは実験0003の4つのバグ全ての発生源だった）。

### 配列ジョブのインデックスは 1 以上（実機で観測）

```
qsub: Array job indices must be greater than 0.
      [-J 0-7]
```

2026-08-14 観測。**PBS Pro 一般の制約ではない** — HPCMP の公開ドキュメントには
`#PBS -J 0-12:3` で subjob 0 が走る例がある。ABCI 固有のサイト設定である。
**ウェブが「動くはず」と言っても、機械が拒否したならそれが事実。**

プランのタスク ID は 0 始まりのまま維持する（プランはデータの記述であって
キューの記述ではない）。`submit_production.sh` が範囲を +1 して
`OD_TASK_ID_OFFSET` を生成ジョブに記録し、`production_job.sh` が引き戻す。
**2つの番号体系が出会う場所は1箇所だけにする。** ここで off-by-one を起こすと
全サブジョブが別タスクを実行し、行数チェックも health チェックも通ってしまう。

`tests/stubs/qsub` がこの制約を再現するので、以後はローカルで捕まる。

### 配列ジョブの状態 `B` は正常（ABCI ドキュメント未記載）

`qstat` の S 列が `B` になるのは配列ジョブの**親**で、「サブジョブが1つ以上 Q を
抜けた」という意味。**対応不要。**

**`B` は `R` にならない。** 親の遷移は `Q → B → F` のみ。`R` は**サブジョブだけ**が
持つ状態で、親には付かない（PBS リファレンスガイドは Job Array States と
Subjob States を別の表として定義している）。`R` を待つと永久に待つ。
親が `F` になるのは**最後のサブジョブが終了した時**なので、
**親の状態からは進捗が一切読めない。**

ABCI の job-execution ページは R / Q / F / S / E しか定義していない。`B` は
PBS Pro 本体の仕様。**公式ドキュメントが網羅していない領域がある。**

- `B` は**全**サブジョブが Q を抜けるまで続く。大半が終了済みでも親は `B`
- サブジョブの状態は既定では出ない。`qstat -t '<jobid>[]'` で見る
- 配列固有の状態 `X` = サブジョブ完了または削除済み
- **インデックスは順番通りに走らない**
- 進捗は `qstat -t '<jobid>[]'` か、`qstat -f` の `array_indices_remaining`、
  あるいは `DONE.json` の数で見る

**開始時刻の予測手段は無い。** ABCI が記載する `qstat` のオプションは
`-f` `-a` `-x` `-t` `-q` のみで、開始予測の機能は存在しない。
待ちの原因が制限値かどうかは切り分けられる: ユーザーあたり同時実行 200、
rt_HF 全体 736。数十サブジョブ程度ではこれらに触れないので、
待っていれば単にノードの空き待ちである。

**シェルでは `[]` をクォートする。** zsh はグロブとして解釈し `no matches found`
で失敗する（実際に間違ったコマンドを渡した）。

### 確定済みの実測値

| 項目 | 値 | 出典 |
|---|---|---|
| ノードあたり最適プロセス数 | **32**（64は31%悪化） | 実験0002 |
| 32プロセスのスループット | 179〜187 succ/s | 実験0002・0003 |
| yield | 約65% | 同上 |
| DNS 失敗率 | **6.0〜6.2%（並列度・ノード数に依存しない）** | DNS プローブ・0002・0003 |
| 1タスク（100万URL） | 約1ノード時間 | 同上 |

---

## 3.5 渡すコマンドの制約

### 4. コマンドは1行120文字以内。超えるならスクリプトにする

長いコマンドは**貼った時点で改行が入り壊れる**。実害を繰り返している:

  singularity exec --bind "..."          ← ここで切れた
  "....sif" python -c "..."              ← 別コマンドとして実行された

「気をつける」では止まらなかった。**構造で守る:**

- 渡すコマンドは `scripts/od.sh <subcommand>` の形にする。バインドとパスは
  テスト済みのスクリプト側に置く
- 新しい調査が必要になったら、**まずスクリプトを書いてテストする**。
  その場限りのワンライナーを渡さない。調査コードも本番コードと同じ扱い
- どうしても直接渡すなら **120文字以内**。数えること

## 4. 変更の手順

- **`main` に直接コミットしない。** ブランチ → PR → CI green
- **commit 前にブランチを確認**（`git branch --show-current`）
- **push 後にリモート SHA と HEAD の一致を確認**（`git ls-remote`）
- **`git add -A` を使わない。** ファイルを明示する（`.pyc` 混入の原因）
- コミットに `Co-Authored-By: Claude` を付けない
- 直したら**関連ドキュメントを全て追従させる**
- 実験は**事前登録してから実行**。閾値は定数1箇所に置き、
  ドキュメントとの不一致でテストが落ちるようにする

## 5. 識別子

サイト固有の値（グループID・アカウントID・予約ID・絶対パス）を書かない。
**実害あり**: `qstat -Qf` の出力を貼るとき、1行に2つあるグループIDの
**片方だけ**置換して実値を残した。

```bash
bash check_no_identifiers.sh    # 緑を保つこと
bash scripts/install_hooks.sh   # clone 直後に一度
```

**赤が常態化するとガードは信号として機能しなくなる。**
誤検出は `.identifier-allow` に理由付きで登録する。
